namespace ServiceLocator.Player
{
    public enum PlayerMovementState
    {
        NONE,
        IDLE,
        WALK,
        RUN,
        FALL,
    }

    public enum PlayerActionState
    {
        NONE,
        AIM,
        FIRE,
    }
}